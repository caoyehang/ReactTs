// 定义侧边栏基础样式。
export const siderStyle = {
  // 使用 antd 暗色菜单同款背景色。
  backgroundColor: "#001529",
  // 设置侧边栏默认文字颜色。
  color: "#fff",
  // 保持侧边栏展开宽度和组件 width 一致。
  width: 240,
};

// 定义顶部栏基础样式。
export const headerStyle = {
  // 使用半透明白色背景，降低视觉重量。
  backgroundColor: "rgba(255, 255, 255, 0.85)",
  // 使用 antd 默认正文深色。
  color: "rgba(0, 0, 0, 0.88)",
  // 设置顶部栏左右内边距。
  padding: "0 20px",
};
